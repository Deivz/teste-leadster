import { styled } from "styled-components";

export type DivTypes = 'large' | 'normal';

export const DefaultPageContainer = styled.div`
  display: flex;
  flex-direction: column;
  min-height: 100vh;
`